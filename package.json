{
  "name": "headroom-mcp-bridge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "supergateway --stdio \"uvx --from 'headroom-ai[all]' headroom mcp serve\" --port $PORT --baseUrl \"$BASE_URL\" --ssePath /sse --messagePath /message --cors"
  },
  "dependencies": {
    "supergateway": "^3.4.3"
  }
}